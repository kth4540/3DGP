ID3D12Device

	가상 어댑터를 나타내는 인터페이스

	CheckFeatureSupport() : 그래픽 드라이버가 지원하는 특성을 확인

다중 샘플링

	계단현상 - 모니터의 픽셀 크기가 무한히 작지 않기 때문에 나타남

		모니터 해상도를 늘리면 계단 현상이 줄어듬

		모니터 해상도를 늘릴 수 없는 상황이라면?

	슈퍼 샘플링(4X) : 후면 버퍼와 깊이 버퍼 해상도를 화면 크기보다 4배 (2X2) 크게 만들어 렌더링
	렌터링할 후면 버퍼 픽셀, 깊이테스트를 할 픽셀의 개수가 4배가 됨, 프리젠트 할 때 후면 버퍼를 샘플링하여 픽셀 색상 구현

	MSAA(Multi-Sample Anti-Aliasing)(4X) : 슈퍼 샘플링처럼 후면 버퍼와 깊이 버퍼를 화면보다 4배 더 크게 생성
	슈퍼샘플링보다 각 픽셀 색상을 계산하는 시간이 더 적게 걸리는 알고리즘
	다중 샘플링은 화면의 픽셀마다 한 번만 색상을 계산(화면 픽셀의 중심에서)
	서브픽셀의 색상은 서브 픽셀의 가시성과 포함 여부에 따라 결정됨
	가시성 : 각 서브 픽셀이 보이는가? -> 판단을 위해 깊이, 스텐실 검사는 각 서브픽셀마다 수행
	포함 여부 : 각 서브 픽셀의 중심이 다각형 내부에 존재하는가? -> 포함 여부 판단은 각 서브 픽셀마다 수행됨
	계산된 픽셀 색상을 다각형에 포함되고 보이는 모든 서브 픽셀 색상에 복사

	
